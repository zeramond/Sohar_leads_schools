# Never commit .env. The SQLite database lives in data/app.db.
DATABASE_URL="file:../data/app.db"

# Generate with: openssl rand -base64 48
CRM_SESSION_SECRET="replace-with-at-least-32-random-characters"
CRM_ACCESS_PASSWORD="replace-with-a-strong-private-password"

NTFY_SERVER_URL="https://ntfy.sh"
NTFY_TOPIC="replace-with-a-private-random-topic"
NTFY_TOKEN=""
